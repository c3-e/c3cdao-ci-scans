---

- name: Run npm install in the repo root directory
  shell: npm install
  args:
    chdir: "{{ playbook_dir }}/../.."
  register: npm_install_result
  retries: 3
  delay: 5
  until: npm_install_result.rc == 0
