---

- name: Install Vale in Conda environment
  shell: >
    source {{ miniconda_install_path }}/bin/activate {{ miniconda.env_name }} && 
    pip install "vale=={{ vale_version }}"
  args:
    executable: /bin/bash
