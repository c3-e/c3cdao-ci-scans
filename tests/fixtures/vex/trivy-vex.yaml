# Fixture: routes tests/fixtures/vex/alpine-known-cve/main.openvex.json into
# Trivy via the config-file path (`vulnerability.vex`). Spike run 31055413726
# proved BOTH routes work at pin 57a97c7 (v0.35.0): ambient TRIVY_VEX env
# (job- or step-level) and trivy-config: (explicitly wired trivy-config ->
# TRIVY_CONFIG -> --config). The earlier "env doesn't reach the inner step"
# conclusion was confounded by the fixture's arch=arm64 PURL bug (fixed in
# 8a03d3d). T2 wires trivy-config: as the documented, pin-stable route.
vulnerability:
  vex:
    - tests/fixtures/vex/alpine-known-cve/main.openvex.json
