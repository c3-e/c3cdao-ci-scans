# Lint fixture: minimal caller, publish_images left at its default
# (false), so packages: write / images: rules are inert. Clean: ref
# pinned by full commit SHA, permissions: at the workflow level only.
name: Publish Staging Chart

on:
  pull_request:
    types: [closed]

permissions:
  contents: read

jobs:
  publish-staging-chart:
    if: github.event.pull_request.merged == true
    uses: c3-e/c3cdao-ci-scans/.github/workflows/publish-staging-chart.yml@0123456789abcdef0123456789abcdef01234567  # v0.1.0
    with:
      chart_path: chart
